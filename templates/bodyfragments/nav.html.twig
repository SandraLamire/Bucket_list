<nav>
    <ul>
        <li><a href="{{ path('main_home') }}">Accueil</a></li>
        <li> ~ </li>
        <li><a href="{{ path('wish_list') }}">List</a></li>
        <li> ~ </li>
        <li><a href="{{ path('main_about_us') }}">About us</a></li>
        {% if app.user %}
            <li> ~ </li>
            <li><a href="{{ path('wish_add') }}">Add a idea</a></li>
            <li> ~ </li>
            <li><a href="{{ path('app_logout') }}">Logout</a></li>
        {% else %}
            <li> ~ </li>
            <li><a href="{{ path('app_login') }}">Login</a></li>
        {% endif %}
        {% if is_granted('ROLE_ADMIN') %}
            <li> ~ </li>
            <li><a href="{{ path('admin_dashboard') }}">Dashboard</a></li>
        {% endif %}
    </ul>
</nav>